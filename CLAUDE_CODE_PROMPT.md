# Prompt to paste into Claude Code

Copy everything below the line into your first message to Claude Code.

---

I'm Anshul. I've been building a phonics learning artifact for my 3-to-4-year-old kid with another instance of you (running in Cowork mode). That instance was network-locked and could not actually run the audio toolchain — you can. Read `CLAUDE.md` in this directory now; it has the full project handoff: file inventory, architecture, every iteration of the audio approach we tried (and acoustically why each failed), open issues, and a suggested next-step playbook.

The unsolved problem is **audio quality**. The artifact (`SATPIN_StoryMode.html`) is a self-contained phonics website. It plays Microsoft Aria neural voice MP3s generated by `generate_voices.py` (uses free `edge-tts`). When I tap a letter card or word, the audio is supposed to say that thing — but specifically:

- **"cat" sometimes sounds like "kate"** — diagnosed as English sentence-final vowel lengthening, fix attempted via mid-position carrier sentence + auto-trim
- **"X" pronounced as letter names "K, S"** instead of the /ks/ phoneme — TTS limitation; fix attempted via word-based example clips ("Box. Fox. Six.")
- **Lessons may be confusing for adults, let alone 3-year-olds** — UX hasn't been audited by anyone who actually heard the audio + clicked through

**What I want you to do, in this order:**

1. Read `CLAUDE.md` end to end before doing anything else.
2. Run `python3 generate_voices.py` to generate fresh audio.
3. **Actually listen** to the output with `afplay` — try at minimum: `audio/word_cat.mp3` (or .wav after trim), `audio/word_hat.mp3`, `audio/word_box.mp3`, `audio/x.mp3`, `audio/sh.mp3`, `audio/word_the.mp3`. Don't trust acoustic analysis alone — listen.
4. Install Whisper (`pip3 install openai-whisper`) and **transcribe each generated word file**. Compare the transcription to the expected word. If they don't match, the carrier sentence or trim is broken — iterate `generate_voices.py` until they do.
5. Once audio is solid, run `./start.command` to launch the artifact in a browser, walk through all 10 lessons, and tell me anything that's confusing or doesn't make sense for a 3-to-4-year-old. Use Chrome DevTools to debug if needed.
6. Report back with concrete findings and what you fixed.

**Hard constraints:**
- I'm not a developer. Walk me through any command I need to run.
- Don't suggest paid services without confirming with me first.
- The artifact must remain a single self-contained HTML file with no build step.
- Voice must be a real human-quality AI voice (Aria via edge-tts is fine; ElevenLabs/Azure free tier is fine if Aria fails).

**Success criteria:**
When I tap "cat" in the artifact, I hear something that unambiguously sounds like /kæt/ — not /keɪt/, not "K, A, T" letter spelling, not robotic synthesis. Same for every other letter and word. And the lesson flow is intuitive enough that a 4-year-old can use it (with a parent reading the prompts).

Begin by reading `CLAUDE.md`.
